{
  "name": "rev-financial-api",
  "version": "1.0.0",
  "description": "An API for a fake financial institution where users can do many basic tasks",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "keywords": [
    "nodejs",
    "api",
    "fintech"
  ],
  "author": "Danny Lim",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^27.0.3",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "tslint": "^6.1.3",
    "typescript": "^4.5.2"
  }
}